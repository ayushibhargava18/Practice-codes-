#include <iostream>
#include <algorithm>
using namespace std;

int main(){
    int arr[]={12,23,45,11};
    int n=sizeof(arr)/sizeof(arr[0]);
    int largest=arr[0];
    int secondlargest=arr[0];
    for(int i=1;i<n;i++){
        if(arr[i]>largest){
            largest=arr[i];
        }
        else{
            continue;
        }
}
    for(int i=1;i<n;i++){
        if(arr[i]>secondlargest){
            if(arr[i]!=largest){
                secondlargest=arr[i];
            }
        }
        else{
            continue;
        }
    
}
cout<<secondlargest;
}
